#use <string>

int hash_string(string s) {
    int result = 0;
    for(int i = 0; i < string_length(s)-1; i++)
    {
        result = result + (683524234 * char_ord(string_charat(s,i)) + 40152023);
    }
    return result; // IMPLEMENT ME
}